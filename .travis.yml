#.Travis.yml
install: gradle wrapper --gradle-version 4.2

language: java
jdk:
  - openjdk8

branches:
  only:
    - chang

# Travis CI 서버의 Home
cache:
  directories:
#     - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

#CI 실행 완료 시 메일로 알람
notifications:
  email:
    recipients:
      - aowert6518@naver.com
